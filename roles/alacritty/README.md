# Alacritty

Installs [Alacritty](https://alacritty.org/) terminal emulator.

Currently only implemented for **WSL** environments - installs via winget on Windows host and deploys WSL-optimized config to `%APPDATA%\alacritty\alacritty.yml`.

## Usage

```bash
dotfiles -t alacritty
```
