interface:
  display_name: "Commit"
  short_description: "Create a conventional commit from repo changes"
  default_prompt: "Use $commit to commit staged changes, or use --all when the user explicitly wants all current changes staged first."
