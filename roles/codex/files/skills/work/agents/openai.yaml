interface:
  display_name: "Work"
  short_description: "Route complex work with focus"
  default_prompt: "Use $work to route this complex engineering task into the smallest effective execution shape."

policy:
  allow_implicit_invocation: false
