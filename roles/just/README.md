# Just

Installs [just](https://github.com/casey/just), a command runner (like make but simpler).

Currently macOS only (Homebrew).

## Usage

```bash
dotfiles -t just
just  # Run in directory with a justfile
```
