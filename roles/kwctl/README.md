# kwctl

Installs [kwctl](https://github.com/kubewarden/kwctl), the Kubewarden CLI for managing WebAssembly-based Kubernetes admission policies.

Ubuntu only (GitHub releases to `/usr/local/bin/kwctl`).

## Usage

```bash
dotfiles -t kwctl
kwctl --help
```
