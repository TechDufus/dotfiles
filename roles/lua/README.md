# Lua

Installs [Lua](https://www.lua.org/), LuaJIT, and LuaRocks.

- **macOS**: Homebrew (`lua`, `luajit`, `luarocks`)
- **Ubuntu**: APT (`lua5.1`, `luajit`, `luarocks`)

Used by Neovim and AwesomeWM configurations.

## Usage

```bash
dotfiles -t lua
luarocks install <module>
```
