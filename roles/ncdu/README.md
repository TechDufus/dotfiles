# ncdu

Installs [ncdu](https://dev.yorhel.nl/ncdu), an ncurses disk usage analyzer.

Supported on macOS (Homebrew), Ubuntu (APT), Fedora (DNF), and Arch (Pacman).

## Usage

```bash
dotfiles -t ncdu
ncdu /path/to/analyze
```
