# TShark

Installs [TShark](https://www.wireshark.org/docs/man-pages/tshark.html), the command-line network protocol analyzer.

Ubuntu only (APT).

## Usage

```bash
dotfiles -t tshark
tshark -i eth0  # Capture on interface
```
